@using Dominio;
@using Microsoft.AspNetCore.Http;
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<h1>Partidos</h1>
<div>
    <h2>Partidos Fase de Grupos</h2>
    <div>
        <h3>Finalizados</h3>
        @foreach (Partido item in ViewBag.PartidosGrupo)
        {
            if (item.Finalizado)
            {
                <div class="card text-center my-3 col-6">
                    <div class="card-header">
                        <h5 class="card-title bg-dark text-white">@item.SeleccionA.Pais.Nombre vs @item.SeleccionB.Pais.Nombre</h5>
                    </div>
                    <div class="card-body">
                        
                        <p class="card-text">@item.Resultado</p>
                    </div>
                    <div class="card-footer text-muted">
                        @item.FechaPartido.ToShortDateString()
                    </div>
                </div>
            }
        }
    </div>

    <div>

        @if (Context.Session.GetString("rol") == "OPERADOR")
        {
            <h3>Por finalizar</h3>
            @foreach (Partido item in ViewBag.PartidosGrupo)
            {
                if (!item.Finalizado)
                {
                    <div class="card text-center my-3 col-6">
                        <div class="card-header">
                            <h5 class="card-title bg-success text-white">@item.SeleccionA.Pais.Nombre vs @item.SeleccionB.Pais.Nombre</h5>
                        </div>
                        <div class="card-body">

                            <a class="btn btn-primary" href="/Partido/FinalizarPartido?id=@item.IDPartido">Finalizar</a>
                        </div>
                        <div class="card-footer text-muted">
                            @item.FechaPartido.ToShortDateString()
                        </div>
                    </div>

                }
            }
        }

    </div>
</div>

<div>
    <h2>Partidos Fase Eliminatorias</h2>
    <div>
        <h3>Finalizados</h3>
        @foreach (Partido item in ViewBag.PartidosEliminatoria)
        {
            if (item.Finalizado)
            {
                <div class="card text-center my-3 col-6">
                    <div class="card-header">
                        <h5 class="card-title bg-dark text-white">@item.SeleccionA.Pais.Nombre vs @item.SeleccionB.Pais.Nombre</h5>
                    </div>
                    <div class="card-body">

                        <p class="card-text">@item.Resultado</p>
                    </div>
                    <div class="card-footer text-muted">
                        @item.FechaPartido.ToShortDateString()
                    </div>
                </div>
            }
        }
    </div>

    <div>
        @if (Context.Session.GetString("rol") == "OPERADOR")
        {
            <h3>Por finalizar</h3>
            @foreach (Partido item in ViewBag.PartidosEliminatoria)
            {
                if (!item.Finalizado)
                {
                    <div class="card text-center my-3 col-6">
                        <div class="card-header bg-success text-white">
                            <h5 class="card-title">@item.SeleccionA.Pais.Nombre vs @item.SeleccionB.Pais.Nombre</h5>
                        </div>
                        <div class="card-body">

                            <a class="btn btn-primary" href="/Partido/FinalizarPartido?id=@item.IDPartido">Finalizar</a>
                        </div>
                        <div class="card-footer text-muted">
                            @item.FechaPartido.ToShortDateString()
                        </div>
                    </div>

                }
            }
        }

    </div>
</div>