@using Dominio;
@{
    ViewData["Title"] = "Home Page";
}

<div>
    <h1 class="display-4">Bienvenido</h1>
    <p>Por favor, inicie sesión</p>
</div>
<div>
    @if (@ViewBag.Error != null)
    {
        <div class="alert alert-danger" role="alert">
            @ViewBag.Error
        </div>
    }
    <form method="post">
        <div class="form-group">
            <label for="Email">Email</label>
            <input type="text" name="email" class="form-control" />
        </div>
        <div class="form-group">
            <label for="Password">Contraseña</label>
            <input type="password" name="password" class="form-control" />
        </div>
        <button type="submit" class="btn btn-primary">Iniciar sesión</button>
    </form>
    <a href="/periodista/CrearPeriodista">Registrarse como periodista.</a>
</div>


@*<h1>Selecciones</h1> ESTO DA UN ERROR CUANDO QUERES HACER LOGIN Y ESTÁN LOS CAMPOS VACÍOS
<div>
    @foreach (Seleccion item in ViewBag.Selecciones)
    {
        <div class="mt-3">
            <hr />
            <h2>@item.Pais.Nombre (@item.Pais.Alpha)</h2>
            <div class="d-flex row container-fluid justify-content-evenly">
                @foreach (Jugador jugador in item.Jugadores)
                {
                    <p class="col-4">@jugador.NombreCompleto</p>
                }
            </div>
        </div>

    }
</div>*@


